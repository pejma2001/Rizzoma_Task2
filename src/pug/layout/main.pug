include ../utils/mixins
block variables

doctype html
html(lang='en')
  head
    meta(charset='utf-8')
    meta(name="viewport" content="width=device-width")
    title #{title}
    meta(name="description", content=description)
    block head

  body(class=pageClass)
    .wrapper

      include ../includes/modules/headerLogin

      .content-wrapper
        block content
      include ../includes/modules/footer

    block script